create table customer(
	cust_id int primary key,
	fname varchar(20),
	lname varchar(20),
	email varchar(20),
	phone varchar(10),
	street varchar(25),
	city   varchar(25),
	state  varchar(2),
	zip    varchar(5)
);
